import { ArrowType, LineType, Placement } from '../enum';
import { Actor } from './Actor';
import { ISequence, SequenceType } from './IActor';
export declare class Signal implements ISequence {
    type: SequenceType;
    actorA: Actor;
    actorB: Actor;
    lineType: LineType;
    arrowType: ArrowType;
    message: string;
    placement: Placement;
    constructor(actorA: Actor, signalType: number, actorB: Actor, message: string);
    isSelf(): boolean;
}
