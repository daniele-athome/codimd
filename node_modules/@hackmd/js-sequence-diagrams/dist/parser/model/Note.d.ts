import { ArrowType, LineType, Placement } from '../enum';
import { Actor } from './Actor';
import { ISequence, SequenceType } from './IActor';
export declare class Note implements ISequence {
    type: SequenceType;
    actor: Actor | Actor[];
    placement: Placement;
    message: string;
    actorA: Actor;
    actorB: Actor;
    arrowType: ArrowType;
    lineType: LineType;
    constructor(actor: Actor | Actor[], placement: Placement, message: string);
    hasManyActors(): boolean;
}
