import { Actor } from '../../parser/model';
import { BaseDrawer } from '../drawer/BaseDrawer';
import { DrawingBox } from './GraphicBox';
import { IDrawableGraphic } from './interfaces';
export declare const ACTOR_MARGIN = 10;
export declare const ACTOR_PADDING = 10;
export declare class ActorGraphic implements IDrawableGraphic {
    box: DrawingBox;
    message: string;
    distance: number[];
    paddingRight: number;
    actor: Actor;
    index: number;
    signalHeight: number;
    constructor(actor: Actor);
    draw(drawer: BaseDrawer): void;
}
