import { Diagram } from '../parser/diagram';
export interface IDiagramPainterDrawSvgOptions {
    theme: 'hand' | 'simple';
}
export declare class DiagramPainter {
    private readonly diagram;
    constructor(diagram: Diagram);
    drawSvg(container: string | HTMLElement, options?: IDiagramPainterDrawSvgOptions): void;
}
