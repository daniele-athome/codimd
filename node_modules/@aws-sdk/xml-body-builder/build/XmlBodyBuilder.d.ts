import { BodySerializer, BodySerializerBuildOptions, Decoder, Encoder } from "@aws-sdk/types";
export declare class XmlBodyBuilder implements BodySerializer {
    private readonly base64Encoder;
    private readonly utf8Decoder;
    constructor(base64Encoder: Encoder, utf8Decoder: Decoder);
    build({ operation, member, hasPayload, input, memberName }: BodySerializerBuildOptions): string;
    private toXml;
    private serialize;
    private serializeStructure;
    private serializeStructureMember;
    private serializeMap;
    private formatMap;
    private serializeList;
    private serializeScalar;
    private serializeBlob;
    private serializeFloat;
    private serializeInteger;
    private serializeString;
    private serializeTimestamp;
}
