import { Middleware, Provider } from "@aws-sdk/types";
export interface LocationConstraintAwareInput {
    CreateBucketConfiguration?: {
        LocationConstraint?: string;
    };
}
export declare function locationConstraintMiddleware<Input extends LocationConstraintAwareInput, Output extends object>(regionProvider: Provider<string>): Middleware<Input, Output>;
