export interface BucketHostnameParameters {
    accelerateEndpoint?: boolean;
    baseHostname: string;
    bucketName: string;
    dualstackEndpoint?: boolean;
    pathStyleEndpoint?: boolean;
    sslCompatible?: boolean;
}
export interface BucketHostname {
    hostname: string;
    bucketEndpoint: boolean;
}
export declare function bucketHostname({ accelerateEndpoint, baseHostname, bucketName, dualstackEndpoint, pathStyleEndpoint, sslCompatible }: BucketHostnameParameters): BucketHostname;
