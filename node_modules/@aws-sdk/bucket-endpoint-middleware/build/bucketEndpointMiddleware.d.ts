import { BuildMiddleware } from "@aws-sdk/types";
export interface BucketEndpointAwareInput {
    Bucket: string;
    $bucketEndpoint?: boolean;
    $forcePathStyle?: boolean;
    $useAccelerateEndpoint?: boolean;
    $useDualstackEndpoint?: boolean;
}
export interface bucketEndpointMiddlewareConfiguration {
    forcePathStyle?: boolean;
    preformedBucketEndpoint?: boolean;
    useAccelerateEndpoint?: boolean;
    useDualstackEndpoint?: boolean;
}
export declare function bucketEndpointMiddleware({ forcePathStyle, preformedBucketEndpoint, useAccelerateEndpoint, useDualstackEndpoint }?: bucketEndpointMiddlewareConfiguration): BuildMiddleware<any, any, any>;
