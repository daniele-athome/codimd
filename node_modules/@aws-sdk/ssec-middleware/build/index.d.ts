import { Decoder, Encoder, Hash, Middleware } from "@aws-sdk/types";
export declare type SsecPropertiesConfiguration<Input extends object> = {
    [sourceProperty in keyof Input]?: {
        targetProperty: string;
        hashTargetProperty: string;
    };
};
export interface SsecMiddlewareConfiguration<Input extends object> {
    utf8Decoder: Decoder;
    base64Encoder: Encoder;
    hashConstructor: {
        new (): Hash;
    };
    ssecProperties: SsecPropertiesConfiguration<Input>;
}
export declare function ssecMiddleware<Input extends object>({ utf8Decoder, base64Encoder, hashConstructor, ssecProperties }: SsecMiddlewareConfiguration<Input>): Middleware<Input, any>;
