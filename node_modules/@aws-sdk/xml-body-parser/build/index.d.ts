import { BodyParser, Decoder, Member } from "@aws-sdk/types";
export declare class XmlBodyParser implements BodyParser {
    private readonly base64Decoder;
    constructor(base64Decoder: Decoder);
    parse<OutputType>(member: Member, input: string): OutputType;
    private unmarshall;
    private parseStructure;
    private mapToXMLKey;
    private parseList;
    private parseMap;
    private parseTimeStamp;
}
